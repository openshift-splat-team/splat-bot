package util

import (
	"testing"
)

func TestRssFeed(t *testing.T) {
	// url := "https://aws.amazon.com/blogs/aws/feed/"
	// items, _ := ParseFeed(url)

	// for _, item := range items {
	// 	spew.Dump(item.Title, item.Published, item.Link, item.Description)
	// }
	// t.Error("booom")
}
