package llm

import (
	"testing"
)

func TestLlama(t *testing.T) {
	//
	/*	jirautil.BindEnvVars()
		ctx := context.TODO()
		issues, err := GetJiraIssueSummary(ctx, `filter = "SPLAT - updates in last week"`)
		if err != nil {
			t.Fatal(err)
		}
		completion, err := Completion(fmt.Sprintf("can summarize these issue titles? %s", issues))
		if err != nil {
			t.Fatal(err)
		}
		spew.Dump(completion)*/
}
